{
  "name": "kaogong-study-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-study-site.mjs",
    "dev": "npm run sync && vitepress dev website --host 0.0.0.0",
    "build": "npm run sync && vitepress build website",
    "preview": "vitepress preview website --host 0.0.0.0"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
